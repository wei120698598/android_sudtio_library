<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.image.lib">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".select_pic.BitmapCache" />
        <activity android:name=".select_pic.ui.AlbumActivity" />
        <activity android:name=".select_pic.ui.GalleryActivity" />
        <activity android:name=".select_pic.ui.ImageFileActivity" />
        <activity android:name=".select_pic.ui.ShowAllPhoto" />
        <activity android:name=".show_pic.image.ImagePagerActivity" />
    </application>

</manifest>
